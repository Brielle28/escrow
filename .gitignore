# Dependencies
node_modules
node_modules/
plan/
report/
# Rust (escrow RISC-V contract)
contracts/escrow-rust/target/

# Build outputs (any package: on-chain dist/, Vite frontend/dist/, etc.)
dist
dist-ssr

# TypeScript / tooling caches
*.tsbuildinfo
.cache
.turbo
coverage

# Local env & secrets (never commit real keys)
.env
.env.*.local
.env.local

# Logs & debug
*.log
pnpm-debug.log*

# OS / editor noise
.DS_Store
Thumbs.db
Desktop.ini
*.swp
*.suo
*.ntvs*
*.njsproj
*.sln

# Optional: uncomment if you generate deploy artifacts locally and do not want them tracked
# /deployment/

# Local tooling binaries (recreated via `pnpm run setup:tools`)
/tools/*.exe
/tools/*.tar.gz
