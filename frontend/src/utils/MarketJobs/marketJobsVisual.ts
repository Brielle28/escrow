/** Hero photography — swap for a dedicated market asset in `public/MarketJobs/` when ready. */
export const marketJobsHeroImage = {
  // src: "/LandingPage/MB.jpg",
  src: "/MarketJob/MJ.png",
  /** Decorative background; empty alt keeps screen readers on the text content. */
  alt: "",
} as const;
